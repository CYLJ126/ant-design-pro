## 样式

我想做一个页面，如图所示，左侧（上方为工具条，下方为编辑区域），右侧为侧边栏：

1. 页面使用响应式布局，根据屏幕宽度和高度自动调整布局。
2. 工具条为一系列按钮，固定高度为40px，宽度与编辑区域一致。
3. 侧边栏默认宽度为450px，左侧和右侧可以使用Splitter组件，实现可调整宽度。
4. 工具条上有按钮，控制编辑区域的显示，默认是单框模式，可支持左右同显，或只显示左侧或右侧框，且同时显示左右框时，支持拖动调整宽度。
5. 编辑区域高度为1000px，固定；
6. 各组件间距为5px。
7. 样式写在.less中，内容写在.tsx中；
